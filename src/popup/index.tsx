import React from 'react'
import ReactDOM from 'react-dom'

const renderPopupContent = () => {
  ReactDOM.render(<h1>This is settings Popup2</h1>, document.querySelector('#root'))
}

renderPopupContent()
